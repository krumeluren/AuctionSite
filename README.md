How to run:

In appsettings ensure connection string and JWT key are set.

Update Database

In swagger or with postman you must sign up or sign in and use the token to access the protected endpoints.
Add the token to the header (or use the authorize button in swagger)
